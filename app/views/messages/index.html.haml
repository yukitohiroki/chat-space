.wrapper
  = render partial: 'messages/side_bar'
  .right-contents
    .header
      .left-header
        .left-header__title
          = @group.name
          %ul.left-header__members
            %li.member Members:
            %li.member 川口,
            %li.member 貴久,
            %li.member kawaguchi,
            %li.member takahisa
      .right-header
        = link_to edit_group_path(@group.id), class: "right-header__button" do
          Edit
    .messages
      = render @messages
    = render partial: 'messages/bottom_content'
